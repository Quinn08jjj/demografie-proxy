{
  "name": "demografie-proxy",
  "version": "1.0.0",
  "description": "Proxy for Anthropic API — keeps API key safe on server",
  "main": "api/evaluate.js",
  "engines": {
    "node": ">=18.0.0"
  }
}
